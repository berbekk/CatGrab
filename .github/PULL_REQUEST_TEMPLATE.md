## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How it was checked

<!-- `swift test` and `swiftlint --strict` run in CI. Say what you verified by hand — especially
     for UI changes, which CI cannot check. Screenshots welcome. -->

- [ ] `swift test` passes
- [ ] `swiftlint lint --strict` is clean
- [ ] Checked by hand in a real build
- [ ] New user-visible strings go through `LocalizationStore` (no hardcoded text in views)
- [ ] `CHANGELOG.md` updated if a user would notice this
